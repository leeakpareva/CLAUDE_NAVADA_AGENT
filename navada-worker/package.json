{
  "name": "navada-edge-worker",
  "version": "1.0.0",
  "description": "NAVADA Edge Cloudflare Worker - API gateway, edge routing, Flix proxy",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.3.0",
    "wrangler": "^4.0.0"
  }
}
